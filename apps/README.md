# apps

Compatibility-only directory for PDF paths.

Active code stays in the simple root folders: `frontend/`, `backend/`, `design-system/`, `shared/`, `docs/`, `daily-notes/`, `infrastructure/`, and `scripts/`.
