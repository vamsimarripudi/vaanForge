# AI

AI generation goes through `ai.service.ts`.

- `AI_PROVIDER=deterministic` uses local deterministic generation for development, tests, and demos.
- Production must use a reviewed provider adapter with real credentials and policy review.
