# System Module

Operational health and launch readiness endpoints.

- `GET /api/v1/system/readiness` reports persistence mode, database configuration, secret readiness, and external provider placeholders.
