# ML Intelligence

No trained ML model is claimed.

Current implementation uses deterministic heuristic engines for:

- Requirement quality
- Project complexity
- Build estimate
- Template recommendation
- Risk scoring
- Error classification
- Anomaly detection

The engine stores score, confidence, reasons, input hash, and engine type. It is designed to be replaced by model-backed providers later.

