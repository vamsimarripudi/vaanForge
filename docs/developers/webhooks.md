# Developer Webhooks

Developer webhooks deliver signed lifecycle events to external systems.

## Supported Events

- `agent.lifecycle`
- `build.completed`
- `deployment.completed`
- `validation.failed`
- `billing.event`
- `workspace.event`

Webhook test sends a real persisted usage/log event and returns a signed test payload.

