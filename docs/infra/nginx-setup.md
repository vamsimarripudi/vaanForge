# Nginx Setup

Nginx should route:

- `app.example.com` to frontend.
- `api.example.com` to backend.
- `meet.example.com` to meeting service.
- `assets.example.com` to asset storage or CDN.

See `infrastructure/nginx/vmnexus.conf`.
