# Server Setup

Target server:

- AWS EC2 Ubuntu.
- Node.js 20+.
- Docker and Docker Compose.
- PostgreSQL.
- Redis temporarily, until Vaanis is ready.
- Nginx reverse proxy.

Production commands will be finalized after domain, repository remote, and environment secrets are available.
